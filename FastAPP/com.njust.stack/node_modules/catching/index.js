
/**
 * Catch promise reject, return as promise resolve
 * @param promise
 */
function catching (promise) {
  return Promise.resolve(promise).catch(function (err) { return err })
}

module.exports = catching